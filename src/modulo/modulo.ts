/**
 * Returns the remainder of the division of two numbers.
 * @param a The dividend number.
 * @returns A function that takes the divisor number and returns the remainder of the division.
 */
export const modulo = (a: number) => (b: number) => a % b;
